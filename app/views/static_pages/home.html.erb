<div id="users-list">
  <% @users.each do |user| %>
    <div class="profile-link">
      <% if user.name.blank? %>
        <%= link_to "#{user.username}", user_path(user) %>
      <% else %>
        <%= link_to "#{user.name}", user_path(user) %>
      <% end %>
    </div>
  <% end %>
</div>
